diff --git a/tools/profiler/src/device_allocation.cu b/tools/profiler/src/device_allocation.cu
index c3e46d80..79cefa96 100644
--- a/tools/profiler/src/device_allocation.cu
+++ b/tools/profiler/src/device_allocation.cu
@@ -32,6 +32,7 @@
    \brief Execution environment
 */
 
+#include <cstdlib>
 #include <cstring>
 
 #include "cutlass/numeric_types.h"
@@ -624,12 +624,21 @@ void DeviceAllocation::initialize_random_device(int seed, Distribution dist) {
     );
     break;
   case library::NumericTypeID::kFE2M1:
-    cutlass::reference::device::BlockFillRandom<cutlass::float_e2m1_t>(
-      reinterpret_cast<cutlass::float_e2m1_t *>(pointer_),
-      capacity_,
-      seed,
-      dist
-    );
+    if (char const *low = std::getenv("BASELINE_NVFP4_PACKED_LOW")) {
+      char const *high = std::getenv("BASELINE_NVFP4_PACKED_HIGH");
+      if (!high) throw std::runtime_error("missing BASELINE_NVFP4_PACKED_HIGH");
+      Distribution packed;
+      packed.set_uniform(std::strtod(low, nullptr), std::strtod(high, nullptr), -1);
+      cutlass::reference::device::BlockFillRandom<uint8_t>(
+        reinterpret_cast<uint8_t *>(pointer_), bytes(), seed, packed
+      );
+    }
+    else {
+      cutlass::reference::device::BlockFillRandom<cutlass::float_e2m1_t>(
+        reinterpret_cast<cutlass::float_e2m1_t *>(pointer_),
+        capacity_, seed, dist
+      );
+    }
     break;
   case library::NumericTypeID::kF64:
     cutlass::reference::device::BlockFillRandom<double>(
diff --git a/tools/profiler/src/device_context.cu b/tools/profiler/src/device_context.cu
index 760a2591..ac14a9ea 100644
--- a/tools/profiler/src/device_context.cu
+++ b/tools/profiler/src/device_context.cu
@@ -31,7 +31,9 @@
 /* \file
    \brief
 */
 
+#include <cstdlib>
+
 #include "cutlass/profiler/device_context.h"
 
 namespace cutlass {
@@ -145,6 +147,25 @@ DeviceAllocation *DeviceContext::allocate_and_initialize_tensor(
   if (options.initialization.enabled) {
     Distribution data_distribution = options.initialization.data_distribution;
 
+    if ((name == "SFA" || name == "SFB") &&
+        type == library::NumericTypeID::kFUE4M3) {
+      char const *low = std::getenv("BASELINE_NVFP4_SCALE_LOW");
+      char const *high = std::getenv("BASELINE_NVFP4_SCALE_HIGH");
+      if (low && high) {
+        data_distribution.set_uniform(
+          std::strtod(low, nullptr), std::strtod(high, nullptr), -1);
+      }
+    }
+    if ((name == "SFA" || name == "SFB") &&
+        type == library::NumericTypeID::kFUE8M0) {
+      char const *low = std::getenv("BASELINE_MXFP8_SCALE_LOW");
+      char const *high = std::getenv("BASELINE_MXFP8_SCALE_HIGH");
+      if (low && high) {
+        data_distribution.set_uniform(
+          std::strtod(low, nullptr), std::strtod(high, nullptr), -1);
+      }
+    }
+
     // check if data distribution is allowed to change
     if(!options.initialization.fix_data_distribution) {
       // change data distribution based on bit width
